# Backend API Configuration
# For local development (Vite dev server), use backend URL directly:
VITE_API_URL=http://localhost:8000

# For production (Docker with nginx proxy), leave empty or don't set:
# VITE_API_URL=
# nginx will proxy /api/ requests to the backend container

# Environment
VITE_NODE_ENV=development

# Debug mode (shows API calls in console)
VITE_DEBUG=true